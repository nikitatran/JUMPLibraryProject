package com.cognixia.jump.models;

/*
 * This interface created so method can be called
 * 		from a generic variable representing one
 * 		of the user types (patron, librarian).
 */
public interface UserWithId {
	
	public int getId();
	
}
